# values from this file are read and set in the application config/config.go
# add more values here and set their types in config/config.go to extend
# EX: api keys, api endpoints/urls, other secrets, configuration values/settings
github:
  github-app-identifier: github-app-identifier-id-here
  github-webhook-secret: "github-webhook-secret-here"
  github-private-key: |
    paste-github-private-rsa-key-here
# LOCAL DEV ONLY: only include in secrets.local.yaml for local development
aws:
  AWS_ACCESS_KEY_ID:
  AWS_SECRET_ACCESS_KEY:
  AWS_SESSION_TOKEN:
  AWS_SECURITY_TOKEN: